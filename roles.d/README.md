# Per-role build flags

Optional. `<role>.flags` is appended to the compiler flags when that role is
built; anything without a file here builds with the defaults, which is what
lets a role MeshCore adds tomorrow build without this repository changing.

Blank lines and `#` comments are ignored.
